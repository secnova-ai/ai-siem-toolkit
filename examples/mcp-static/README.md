# Static MCP example

Replace `tool_name` and the input schema with an actual server's contract. This package demonstrates static tool definitions; `mcp_discovery` is not enabled. Validate and pack locally, then configure credentials and invoke the remote read-only tool in SIEM. The package does not implement or start an MCP server.
